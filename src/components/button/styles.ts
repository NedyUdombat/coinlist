import { StyleSheet } from "react-native";

export default StyleSheet.create({
  iconButton: {
    height: 40,
    width: 40,
    borderRadius: 8,
    justifyContent: "center",
    alignItems: "center",
  },
});
